meta {
  name: {fid}.status.patch
  type: http
}

patch {
  url: {{base_url}}{{api_prefix}}/findings/{{finding_id}}/status
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}

body:json {
  {
    "status": "confirmed"
  }
}
