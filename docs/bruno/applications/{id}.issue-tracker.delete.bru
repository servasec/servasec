meta {
  name: {id}.issue-tracker.delete
  type: http
}

delete {
  url: {{base_url}}{{api_prefix}}/applications/{{app_id}}/issue-tracker
  body: none
  auth: none
}

headers {
  X-CSRF-Token: {{csrf_token}}
}
