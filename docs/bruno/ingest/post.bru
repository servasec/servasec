meta {
  name: post
  type: http
}

post {
  url: {{base_url}}{{api_prefix}}/ingest
  body: multipartForm
  auth: none
}

headers {
  X-Api-Token: {{api_token}}
}

body:multipart-form {
  file: @file(../../test/reports/semgrep.json)
  version: main
  branch: main
}
