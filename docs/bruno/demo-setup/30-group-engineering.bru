meta {
  name: 30-group-engineering
  type: http
  seq: 4
}
post {
  url: {{base_url}}{{api_prefix}}/groups
  body: json
  auth: none
}
headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}
body:json {
  {
    "name": "Engineering {{run_id}}",
    "description": "Engineering & Platform teams",
    "path": "engineering-{{run_id}}"
  }
}
script:post-response {
  bru.setVar("engineering_group_id", res.body.id);
}
