meta {
  name: 75-ingest-auth
  type: http
  seq: 48
}
post {
  url: {{base_url}}{{api_prefix}}/applications/{{auth_service_1_app_id}}/ingest
  body: multipartForm
  auth: none
}
headers {
  X-CSRF-Token: {{csrf_token}}
}
body:multipart-form {
  file: @file(demo-setup/samples/trivy-infra.json)
  version: main
}
