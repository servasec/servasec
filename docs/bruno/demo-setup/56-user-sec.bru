meta {
  name: 56-user-sec
  type: http
  seq: 8
}
post {
  url: {{base_url}}{{api_prefix}}/register
  body: json
  auth: none
}
headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}
body:json {
  {
    "username": "sec-{{run_id}}",
    "email": "sec-{{run_id}}@example.com",
    "password": "Demo123!"
  }
}
script:post-response {
  bru.setVar("sec_user_id", res.body.id);
}
