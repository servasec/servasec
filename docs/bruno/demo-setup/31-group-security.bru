meta {
  name: 31-group-security
  type: http
  seq: 5
}
post {
  url: {{base_url}}{{api_prefix}}/groups
  body: json
  auth: none
}
headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}
body:json {
  {
    "name": "Security {{run_id}}",
    "description": "Security & Compliance team",
    "path": "security-{{run_id}}"
  }
}
script:post-response {
  bru.setVar("security_group_id", res.body.id);
}
