meta {
  name: 55-user-dev
  type: http
  seq: 7
}
post {
  url: {{base_url}}{{api_prefix}}/register
  body: json
  auth: none
}
headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}
body:json {
  {
    "username": "dev-{{run_id}}",
    "email": "dev-{{run_id}}@example.com",
    "password": "Demo123!"
  }
}
script:post-response {
  bru.setVar("dev_user_id", res.body.id);
}
