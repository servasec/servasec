meta {
  name: 71-ingest-portal
  type: http
  seq: 44
}
post {
  url: {{base_url}}{{api_prefix}}/applications/{{customer_portal_1_app_id}}/ingest
  body: multipartForm
  auth: none
}
headers {
  X-CSRF-Token: {{csrf_token}}
}
body:multipart-form {
  file: @file(demo-setup/samples/semgrep-webapp.json)
  version: main
}
