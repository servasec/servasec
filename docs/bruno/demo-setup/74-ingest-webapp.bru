meta {
  name: 74-ingest-webapp
  type: http
  seq: 47
}
post {
  url: {{base_url}}{{api_prefix}}/applications/{{web_app_1_app_id}}/ingest
  body: multipartForm
  auth: none
}
headers {
  X-CSRF-Token: {{csrf_token}}
}
body:multipart-form {
  file: @file(demo-setup/samples/trivy-deps.json)
  version: main
}
