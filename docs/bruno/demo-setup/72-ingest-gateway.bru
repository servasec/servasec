meta {
  name: 72-ingest-gateway
  type: http
  seq: 45
}
post {
  url: {{base_url}}{{api_prefix}}/applications/{{api_gateway_1_app_id}}/ingest
  body: multipartForm
  auth: none
}
headers {
  X-CSRF-Token: {{csrf_token}}
}
body:multipart-form {
  file: @file(demo-setup/samples/semgrep-api.json)
  version: main
}
