meta {
  name: 77-ingest-analytics
  type: http
  seq: 50
}
post {
  url: {{base_url}}{{api_prefix}}/applications/{{analytics_1_app_id}}/ingest
  body: multipartForm
  auth: none
}
headers {
  X-CSRF-Token: {{csrf_token}}
}
body:multipart-form {
  file: @file(demo-setup/samples/semgrep-webapp.json)
  version: develop
}
