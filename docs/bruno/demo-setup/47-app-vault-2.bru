meta {
  name: 47-app-vault-2
  type: http
  seq: 17
}
post {
  url: {{base_url}}{{api_prefix}}/applications
  body: json
  auth: none
}
headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}
body:json {
  {
    "name": "Vault Service 2 {{run_id}}",
    "slug": "vault-service-2-{{run_id}}",
    "description": "Vault Service 2 instance",
    "groupId": {{security_group_id}},
    "repositoryUrl": "https://github.com/servasec/vault-service-2-{{run_id}}",
    "assetCriticality": "critical"
  }
}
script:post-response {
  bru.setVar("vault_service_2_app_id", res.body.id);
}
