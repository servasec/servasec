meta {
  name: 54-version-customer_portal_1-staging
  type: http
  seq: 32
}
post {
  url: {{base_url}}{{api_prefix}}/applications/{{customer_portal_1_app_id}}/versions
  body: json
  auth: none
}
headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}
body:json {
  {
    "name": "staging",
    "tag": "staging",
    "branch": "staging"
  }
}
