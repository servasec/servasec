meta {
  name: admin.{stid}.put
  type: http
}

put {
  url: {{base_url}}{{api_prefix}}/admin/scanner-types/{{scanner_type_id}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}

body:json {
  {
    "enabled": false
  }
}
