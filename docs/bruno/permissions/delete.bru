meta {
  name: delete
  type: http
}

delete {
  url: {{base_url}}{{api_prefix}}/admin/permissions
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}

body:json {
  {
    "subject": "user:2",
    "resource": "/applications/1",
    "action": "read"
  }
}
