meta {
  name: post
  type: http
}

post {
  url: {{base_url}}{{api_prefix}}/api-keys
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}

body:json {
  {
    "name": "CI/CD Pipeline Key",
    "scopes": ["ingest"]
  }
}
