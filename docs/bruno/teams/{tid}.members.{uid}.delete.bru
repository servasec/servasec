meta {
  name: {tid}.members.{uid}.delete
  type: http
}

delete {
  url: {{base_url}}{{api_prefix}}/teams/{{team_id}}/members/{{user_id}}
  body: none
  auth: none
}

headers {
  X-CSRF-Token: {{csrf_token}}
}
